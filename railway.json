{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "gunicorn app:app --bind 0.0.0.0:$PORT --worker-class gthread --workers 1 --threads 8 --timeout 600 --keep-alive 75",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
